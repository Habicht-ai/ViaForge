package com.viaversion.viaforge.items;

/** Animation time exposed by the native skull tile after mixing in 1.9 behavior. */
public interface DragonHeadAnimation {
    float viaForge$jawTime(float partialTicks);
}
